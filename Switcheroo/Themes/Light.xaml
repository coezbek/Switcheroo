<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Colors -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F8F8F8" />
    <SolidColorBrush x:Key="WindowBorderBrush" Color="DarkGray" />
    <SolidColorBrush x:Key="ControlBackgroundBrush" Color="White" />
    <SolidColorBrush x:Key="ControlMouseOverBrush" Color="#E5F3FF" />
    <SolidColorBrush x:Key="ControlPressedBrush" Color="#CCE8FF" />
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="White" />

    <!-- Text -->
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="Black" />
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="Gray" />
    <SolidColorBrush x:Key="FadedTextBrush" Color="Silver" />
    <SolidColorBrush x:Key="HelpIconBrush" Color="DarkGray" />

    <!-- Selection/Highlights -->
    <SolidColorBrush x:Key="ActiveColumnBrush" Color="#F0F8FF" />

    <!-- INTERACTION -->
    <!-- Hover: Light Blue BG + Darker Blue Border -->
    <SolidColorBrush x:Key="WindowItemHoverBackgroundBrush" Color="#E5F3FF" />
    <SolidColorBrush x:Key="WindowItemHoverBorderBrush" Color="#70C0E7" />

    <!-- Selected: Pale Blue BG (fallback) + Strong Blue Border -->
    <SolidColorBrush x:Key="WindowItemSelectedBackgroundBrush" Color="#A8D1F0" /> 
    <SolidColorBrush x:Key="WindowItemSelectedBorderBrush" Color="#0078D7" />
    
    <!-- Selected Inactive: Same as active so it looks persistent -->
    <SolidColorBrush x:Key="WindowItemSelectedInactiveBackgroundBrush" Color="#D9EBF9" />

    <!-- Button Style -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource WindowBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                        <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" TargetName="border" Value="{DynamicResource ControlMouseOverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" TargetName="border" Value="{DynamicResource ControlPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="border" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>