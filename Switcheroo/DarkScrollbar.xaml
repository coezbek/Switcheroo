<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--Scrollbar Thumbs-->
    <Style x:Key="ScrollThumbs" TargetType="{x:Type Thumb}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Grid x:Name="Grid" Background="Transparent">
                        <!-- Visual Thumb: Default is Narrow (2px) and Centered -->
                        <Border x:Name="ThumbVisual" 
                                CornerRadius="4" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Stretch" 
                                Width="2" 
                                Background="{TemplateBinding Background}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- EXPAND: When mouse is over the PARENT ScrollBar track area -->
                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}" Value="True">
                            <Setter TargetName="ThumbVisual" Property="Width" Value="6" />
                        </DataTrigger>
                        
                        <!-- EXPAND: When dragging -->
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbVisual" Property="Width" Value="6" />
                        </Trigger>

                        <!-- Horizontal Orientation Adjustments -->
                        <Trigger Property="Tag" Value="Horizontal">
                            <Setter TargetName="ThumbVisual" Property="Width" Value="Auto" />
                            <Setter TargetName="ThumbVisual" Property="Height" Value="2" />
                            <Setter TargetName="ThumbVisual" Property="HorizontalAlignment" Value="Stretch" />
                            <Setter TargetName="ThumbVisual" Property="VerticalAlignment" Value="Center" />
                        </Trigger>
                        
                        <!-- Horizontal Expand Triggers -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}" Value="True" />
                                <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Horizontal" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="ThumbVisual" Property="Height" Value="6" />
                        </MultiDataTrigger>
                        
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsDragging" Value="True" />
                                <Condition Property="Tag" Value="Horizontal" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ThumbVisual" Property="Height" Value="6" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--ScrollBars-->
    <Style x:Key="DarkScrollBar" TargetType="{x:Type ScrollBar}">
        <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
        <Setter Property="Foreground" Value="#686868" />
        <Setter Property="Background" Value="Transparent" />
        <!-- Set physical width to 12 to create a comfortable hover zone -->
        <Setter Property="Width" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid x:Name="GridRoot" Width="12" Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
                            <Track.Thumb>
                                <!-- Pass Orientation to Thumb via Tag so it knows how to expand -->
                                <Thumb x:Name="Thumb" 
                                       Tag="{Binding Orientation, RelativeSource={RelativeSource TemplatedParent}}"
                                       Background="{TemplateBinding Foreground}" 
                                       Style="{DynamicResource ScrollThumbs}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton x:Name="PageUp" Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="false" />
                            </Track.IncreaseRepeatButton>
                            <Track.DecreaseRepeatButton>
                                <RepeatButton x:Name="PageDown" Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="false" />
                            </Track.DecreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Color on Hover -->
                        <Trigger SourceName="Thumb" Property="IsMouseOver" Value="true">
                            <Setter Value="#9E9E9E" TargetName="Thumb" Property="Background" />
                        </Trigger>
                        <!-- Color on Drag -->
                        <Trigger SourceName="Thumb" Property="IsDragging" Value="true">
                            <Setter Value="#bEbEbE" TargetName="Thumb" Property="Background" />
                        </Trigger>
                        
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="GridRoot" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="-90" />
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="PART_Track" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="-90" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Width" Value="Auto" />
                            <Setter Property="Height" Value="12" />
                            <Setter TargetName="Thumb" Property="Tag" Value="Horizontal" />
                            <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand" />
                            <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>